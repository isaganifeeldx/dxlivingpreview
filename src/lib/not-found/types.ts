import type { SeoData } from '@/lib/seo/types'

export type NotFoundPageContentData = {
  heading: string
  title: string
  description: string
  hint: string
  ctaLabel: string
  ctaHref: string
  seo: SeoData
}
